- if f.object.errors.any?
  .alert.alert-danger{role: "alert"}
    %h4.alert-heading
      =t("errors.template.header", model: f.object.model_name.human, count: f.object.errors.count)
    %hr
    -f.object.errors.full_messages.each do |message|
      %div=message
